{
    "name": "your-vendor/your-project",
    "description": "A brief description of your project",
    "type": "project",
    "require": {
        "php": "^7.4 || ^8.0",  // Specify the PHP version
        "monolog/monolog": "^2.0"  // Example dependency (Monolog for logging)
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5"  // Example dev dependency (PHPUnit for testing)
    },
    "autoload": {
        "psr-4": {
            "YourNamespace\\": "src/"
        }
    },
    "scripts": {
        "test": "phpunit"
    },
    "config": {
        "optimize-autoloader": true
    }
}
